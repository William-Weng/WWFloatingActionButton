import XCTest
@testable import WWFloatingActionButton

final class WWFloatingActionButtonTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(WWFloatingActionButton().text, "Hello, World!")
    }
}
